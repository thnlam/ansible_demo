---

 - hosts: all
   become: true
   tasks:

   - name: uninstall apache2 package
     apt:
      name: apache2
      state: absent
   - name: uninstall vim-nox package
     apt:
      name: vim-nox
      state: absent
